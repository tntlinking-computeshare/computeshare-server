# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Dashboard API
    version: 0.0.1
paths:
    /v1/dashboard/cycles/count:
        get:
            tags:
                - Dashboard
            description: 已发放积分总数 回收积分总数 发放代金券总数 已充值总数
            operationId: Dashboard_CyclesCount
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CyclesCountReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/dashboard/gateways/count:
        get:
            tags:
                - Dashboard
            description: Gateway总数
            operationId: Dashboard_GatewaysCount
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GatewaysCountReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/dashboard/gateways/list:
        get:
            tags:
                - Dashboard
            description: Gateway列表 总端口数 已用端口数
            operationId: Dashboard_GatewaysList
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GatewaysListReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/dashboard/providers/count:
        get:
            tags:
                - Dashboard
            description: Provider总数
            operationId: Dashboard_ProvidersCount
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ProvidersCountReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/dashboard/providers/list:
        get:
            tags:
                - Dashboard
            description: Provider列表 类型，总数，规格，是否存活
            operationId: Dashboard_ProvidersList
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ProvidersListReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/dashboard/sandbox/count:
        get:
            tags:
                - Dashboard
            description: 沙箱调用总数
            operationId: Dashboard_SandboxCount
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/SandboxCountReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/dashboard/storages/count:
        get:
            tags:
                - Dashboard
            description: 存储总数 已使用总数
            operationId: Dashboard_StoragesCount
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/StoragesCountReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        CyclesCountReply:
            type: object
            properties:
                code:
                    type: integer
                    format: int32
                message:
                    type: string
                data:
                    $ref: '#/components/schemas/CyclesCountReply_CyclesCount'
        CyclesCountReply_CyclesCount:
            type: object
            properties:
                grantTotal:
                    type: string
                recoveryTotal:
                    type: string
                grantVouchersTotal:
                    type: string
                rechargedTotal:
                    type: string
        GatewaysCountReply:
            type: object
            properties:
                code:
                    type: integer
                    format: int32
                message:
                    type: string
                data:
                    type: integer
                    format: int32
        GatewaysListReply:
            type: object
            properties:
                code:
                    type: integer
                    format: int32
                message:
                    type: string
                data:
                    type: array
                    items:
                        $ref: '#/components/schemas/GatewaysListReply_GatewaysList'
        GatewaysListReply_GatewaysList:
            type: object
            properties:
                name:
                    type: string
                ip:
                    type: string
                totalPort:
                    type: integer
                    format: int32
                useIntranetPort:
                    type: integer
                    format: int32
                usePublicPort:
                    type: integer
                    format: int32
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        ProvidersCountReply:
            type: object
            properties:
                code:
                    type: integer
                    format: int32
                message:
                    type: string
                data:
                    type: integer
                    format: int32
        ProvidersListReply:
            type: object
            properties:
                code:
                    type: integer
                    format: int32
                message:
                    type: string
                data:
                    type: array
                    items:
                        $ref: '#/components/schemas/ProvidersListReply_ProvidersList'
        ProvidersListReply_ProvidersList:
            type: object
            properties:
                type:
                    type: string
                mac:
                    type: string
                ip:
                    type: string
                totalCpu:
                    type: integer
                    format: int32
                totalMemory:
                    type: integer
                    format: int32
                active:
                    type: boolean
        SandboxCountReply:
            type: object
            properties:
                code:
                    type: integer
                    format: int32
                message:
                    type: string
                data:
                    type: integer
                    format: int32
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        StoragesCountReply:
            type: object
            properties:
                code:
                    type: integer
                    format: int32
                message:
                    type: string
                data:
                    type: integer
                    format: int32
tags:
    - name: Dashboard
