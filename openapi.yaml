# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Sandbox API
    version: 0.0.1
paths:
    /v1/sandbox:
        post:
            tags:
                - Sandbox
            operationId: Sandbox_CreateSandbox
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateSandboxRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CreateSandboxReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        CreateInstanceRequest:
            type: object
            properties:
                specId:
                    type: integer
                    format: int32
                imageId:
                    type: integer
                    format: int32
                duration:
                    type: integer
                    format: int32
                name:
                    type: string
                publicKey:
                    type: string
                password:
                    type: string
                dockerCompose:
                    type: string
        CreateNetworkMappingRequest:
            type: object
            properties:
                name:
                    type: string
                protocol:
                    type: string
                computerId:
                    type: string
                computerPort:
                    type: integer
                    format: int32
        CreateSandboxReply:
            type: object
            properties:
                code:
                    type: integer
                    format: int32
                message:
                    type: string
                data:
                    $ref: '#/components/schemas/CreateSandboxReply_CreateSandboxReply_Data'
        CreateSandboxReply_CreateSandboxReply_Data:
            type: object
            properties:
                instanceId:
                    type: string
                networkMappings:
                    type: array
                    items:
                        $ref: '#/components/schemas/CreateSandboxReply_CreateSandbox_NetworkMapping'
        CreateSandboxReply_CreateSandbox_NetworkMapping:
            type: object
            properties:
                id:
                    type: string
                name:
                    type: string
                computerPort:
                    type: integer
                    format: int32
                serverIp:
                    type: string
                serverPort:
                    type: integer
                    format: int32
        CreateSandboxRequest:
            type: object
            properties:
                instance:
                    $ref: '#/components/schemas/CreateInstanceRequest'
                networkMapping:
                    type: array
                    items:
                        $ref: '#/components/schemas/CreateNetworkMappingRequest'
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: Sandbox
