// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/mohaijiang/computeshare-server/internal/data/ent/cycleredeemcode"
	"github.com/mohaijiang/computeshare-server/internal/data/ent/predicate"
)

// CycleRedeemCodeQuery is the builder for querying CycleRedeemCode entities.
type CycleRedeemCodeQuery struct {
	config
	ctx        *QueryContext
	order      []cycleredeemcode.OrderOption
	inters     []Interceptor
	predicates []predicate.CycleRedeemCode
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CycleRedeemCodeQuery builder.
func (crcq *CycleRedeemCodeQuery) Where(ps ...predicate.CycleRedeemCode) *CycleRedeemCodeQuery {
	crcq.predicates = append(crcq.predicates, ps...)
	return crcq
}

// Limit the number of records to be returned by this query.
func (crcq *CycleRedeemCodeQuery) Limit(limit int) *CycleRedeemCodeQuery {
	crcq.ctx.Limit = &limit
	return crcq
}

// Offset to start from.
func (crcq *CycleRedeemCodeQuery) Offset(offset int) *CycleRedeemCodeQuery {
	crcq.ctx.Offset = &offset
	return crcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (crcq *CycleRedeemCodeQuery) Unique(unique bool) *CycleRedeemCodeQuery {
	crcq.ctx.Unique = &unique
	return crcq
}

// Order specifies how the records should be ordered.
func (crcq *CycleRedeemCodeQuery) Order(o ...cycleredeemcode.OrderOption) *CycleRedeemCodeQuery {
	crcq.order = append(crcq.order, o...)
	return crcq
}

// First returns the first CycleRedeemCode entity from the query.
// Returns a *NotFoundError when no CycleRedeemCode was found.
func (crcq *CycleRedeemCodeQuery) First(ctx context.Context) (*CycleRedeemCode, error) {
	nodes, err := crcq.Limit(1).All(setContextOp(ctx, crcq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{cycleredeemcode.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (crcq *CycleRedeemCodeQuery) FirstX(ctx context.Context) *CycleRedeemCode {
	node, err := crcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CycleRedeemCode ID from the query.
// Returns a *NotFoundError when no CycleRedeemCode ID was found.
func (crcq *CycleRedeemCodeQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = crcq.Limit(1).IDs(setContextOp(ctx, crcq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{cycleredeemcode.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (crcq *CycleRedeemCodeQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := crcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CycleRedeemCode entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CycleRedeemCode entity is found.
// Returns a *NotFoundError when no CycleRedeemCode entities are found.
func (crcq *CycleRedeemCodeQuery) Only(ctx context.Context) (*CycleRedeemCode, error) {
	nodes, err := crcq.Limit(2).All(setContextOp(ctx, crcq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{cycleredeemcode.Label}
	default:
		return nil, &NotSingularError{cycleredeemcode.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (crcq *CycleRedeemCodeQuery) OnlyX(ctx context.Context) *CycleRedeemCode {
	node, err := crcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CycleRedeemCode ID in the query.
// Returns a *NotSingularError when more than one CycleRedeemCode ID is found.
// Returns a *NotFoundError when no entities are found.
func (crcq *CycleRedeemCodeQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = crcq.Limit(2).IDs(setContextOp(ctx, crcq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{cycleredeemcode.Label}
	default:
		err = &NotSingularError{cycleredeemcode.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (crcq *CycleRedeemCodeQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := crcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CycleRedeemCodes.
func (crcq *CycleRedeemCodeQuery) All(ctx context.Context) ([]*CycleRedeemCode, error) {
	ctx = setContextOp(ctx, crcq.ctx, "All")
	if err := crcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CycleRedeemCode, *CycleRedeemCodeQuery]()
	return withInterceptors[[]*CycleRedeemCode](ctx, crcq, qr, crcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (crcq *CycleRedeemCodeQuery) AllX(ctx context.Context) []*CycleRedeemCode {
	nodes, err := crcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CycleRedeemCode IDs.
func (crcq *CycleRedeemCodeQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if crcq.ctx.Unique == nil && crcq.path != nil {
		crcq.Unique(true)
	}
	ctx = setContextOp(ctx, crcq.ctx, "IDs")
	if err = crcq.Select(cycleredeemcode.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (crcq *CycleRedeemCodeQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := crcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (crcq *CycleRedeemCodeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, crcq.ctx, "Count")
	if err := crcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, crcq, querierCount[*CycleRedeemCodeQuery](), crcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (crcq *CycleRedeemCodeQuery) CountX(ctx context.Context) int {
	count, err := crcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (crcq *CycleRedeemCodeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, crcq.ctx, "Exist")
	switch _, err := crcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (crcq *CycleRedeemCodeQuery) ExistX(ctx context.Context) bool {
	exist, err := crcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CycleRedeemCodeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (crcq *CycleRedeemCodeQuery) Clone() *CycleRedeemCodeQuery {
	if crcq == nil {
		return nil
	}
	return &CycleRedeemCodeQuery{
		config:     crcq.config,
		ctx:        crcq.ctx.Clone(),
		order:      append([]cycleredeemcode.OrderOption{}, crcq.order...),
		inters:     append([]Interceptor{}, crcq.inters...),
		predicates: append([]predicate.CycleRedeemCode{}, crcq.predicates...),
		// clone intermediate query.
		sql:  crcq.sql.Clone(),
		path: crcq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		FkUserID uuid.UUID `json:"fk_user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CycleRedeemCode.Query().
//		GroupBy(cycleredeemcode.FieldFkUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (crcq *CycleRedeemCodeQuery) GroupBy(field string, fields ...string) *CycleRedeemCodeGroupBy {
	crcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CycleRedeemCodeGroupBy{build: crcq}
	grbuild.flds = &crcq.ctx.Fields
	grbuild.label = cycleredeemcode.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		FkUserID uuid.UUID `json:"fk_user_id,omitempty"`
//	}
//
//	client.CycleRedeemCode.Query().
//		Select(cycleredeemcode.FieldFkUserID).
//		Scan(ctx, &v)
func (crcq *CycleRedeemCodeQuery) Select(fields ...string) *CycleRedeemCodeSelect {
	crcq.ctx.Fields = append(crcq.ctx.Fields, fields...)
	sbuild := &CycleRedeemCodeSelect{CycleRedeemCodeQuery: crcq}
	sbuild.label = cycleredeemcode.Label
	sbuild.flds, sbuild.scan = &crcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CycleRedeemCodeSelect configured with the given aggregations.
func (crcq *CycleRedeemCodeQuery) Aggregate(fns ...AggregateFunc) *CycleRedeemCodeSelect {
	return crcq.Select().Aggregate(fns...)
}

func (crcq *CycleRedeemCodeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range crcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, crcq); err != nil {
				return err
			}
		}
	}
	for _, f := range crcq.ctx.Fields {
		if !cycleredeemcode.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if crcq.path != nil {
		prev, err := crcq.path(ctx)
		if err != nil {
			return err
		}
		crcq.sql = prev
	}
	return nil
}

func (crcq *CycleRedeemCodeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CycleRedeemCode, error) {
	var (
		nodes = []*CycleRedeemCode{}
		_spec = crcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CycleRedeemCode).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CycleRedeemCode{config: crcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, crcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (crcq *CycleRedeemCodeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := crcq.querySpec()
	_spec.Node.Columns = crcq.ctx.Fields
	if len(crcq.ctx.Fields) > 0 {
		_spec.Unique = crcq.ctx.Unique != nil && *crcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, crcq.driver, _spec)
}

func (crcq *CycleRedeemCodeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(cycleredeemcode.Table, cycleredeemcode.Columns, sqlgraph.NewFieldSpec(cycleredeemcode.FieldID, field.TypeUUID))
	_spec.From = crcq.sql
	if unique := crcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if crcq.path != nil {
		_spec.Unique = true
	}
	if fields := crcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, cycleredeemcode.FieldID)
		for i := range fields {
			if fields[i] != cycleredeemcode.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := crcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := crcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := crcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := crcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (crcq *CycleRedeemCodeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(crcq.driver.Dialect())
	t1 := builder.Table(cycleredeemcode.Table)
	columns := crcq.ctx.Fields
	if len(columns) == 0 {
		columns = cycleredeemcode.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if crcq.sql != nil {
		selector = crcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if crcq.ctx.Unique != nil && *crcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range crcq.predicates {
		p(selector)
	}
	for _, p := range crcq.order {
		p(selector)
	}
	if offset := crcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := crcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CycleRedeemCodeGroupBy is the group-by builder for CycleRedeemCode entities.
type CycleRedeemCodeGroupBy struct {
	selector
	build *CycleRedeemCodeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (crcgb *CycleRedeemCodeGroupBy) Aggregate(fns ...AggregateFunc) *CycleRedeemCodeGroupBy {
	crcgb.fns = append(crcgb.fns, fns...)
	return crcgb
}

// Scan applies the selector query and scans the result into the given value.
func (crcgb *CycleRedeemCodeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, crcgb.build.ctx, "GroupBy")
	if err := crcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CycleRedeemCodeQuery, *CycleRedeemCodeGroupBy](ctx, crcgb.build, crcgb, crcgb.build.inters, v)
}

func (crcgb *CycleRedeemCodeGroupBy) sqlScan(ctx context.Context, root *CycleRedeemCodeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(crcgb.fns))
	for _, fn := range crcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*crcgb.flds)+len(crcgb.fns))
		for _, f := range *crcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*crcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := crcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CycleRedeemCodeSelect is the builder for selecting fields of CycleRedeemCode entities.
type CycleRedeemCodeSelect struct {
	*CycleRedeemCodeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (crcs *CycleRedeemCodeSelect) Aggregate(fns ...AggregateFunc) *CycleRedeemCodeSelect {
	crcs.fns = append(crcs.fns, fns...)
	return crcs
}

// Scan applies the selector query and scans the result into the given value.
func (crcs *CycleRedeemCodeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, crcs.ctx, "Select")
	if err := crcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CycleRedeemCodeQuery, *CycleRedeemCodeSelect](ctx, crcs.CycleRedeemCodeQuery, crcs, crcs.inters, v)
}

func (crcs *CycleRedeemCodeSelect) sqlScan(ctx context.Context, root *CycleRedeemCodeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(crcs.fns))
	for _, fn := range crcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*crcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := crcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
