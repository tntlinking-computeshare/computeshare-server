// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/mohaijiang/computeshare-server/internal/data/ent/predicate"
	"github.com/mohaijiang/computeshare-server/internal/data/ent/s3bucket"
	"github.com/mohaijiang/computeshare-server/internal/data/ent/s3user"
)

// S3UserQuery is the builder for querying S3User entities.
type S3UserQuery struct {
	config
	ctx         *QueryContext
	order       []s3user.OrderOption
	inters      []Interceptor
	predicates  []predicate.S3User
	withBuckets *S3BucketQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the S3UserQuery builder.
func (sq *S3UserQuery) Where(ps ...predicate.S3User) *S3UserQuery {
	sq.predicates = append(sq.predicates, ps...)
	return sq
}

// Limit the number of records to be returned by this query.
func (sq *S3UserQuery) Limit(limit int) *S3UserQuery {
	sq.ctx.Limit = &limit
	return sq
}

// Offset to start from.
func (sq *S3UserQuery) Offset(offset int) *S3UserQuery {
	sq.ctx.Offset = &offset
	return sq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sq *S3UserQuery) Unique(unique bool) *S3UserQuery {
	sq.ctx.Unique = &unique
	return sq
}

// Order specifies how the records should be ordered.
func (sq *S3UserQuery) Order(o ...s3user.OrderOption) *S3UserQuery {
	sq.order = append(sq.order, o...)
	return sq
}

// QueryBuckets chains the current query on the "buckets" edge.
func (sq *S3UserQuery) QueryBuckets() *S3BucketQuery {
	query := (&S3BucketClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(s3user.Table, s3user.FieldID, selector),
			sqlgraph.To(s3bucket.Table, s3bucket.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, s3user.BucketsTable, s3user.BucketsColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first S3User entity from the query.
// Returns a *NotFoundError when no S3User was found.
func (sq *S3UserQuery) First(ctx context.Context) (*S3User, error) {
	nodes, err := sq.Limit(1).All(setContextOp(ctx, sq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{s3user.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sq *S3UserQuery) FirstX(ctx context.Context) *S3User {
	node, err := sq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first S3User ID from the query.
// Returns a *NotFoundError when no S3User ID was found.
func (sq *S3UserQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = sq.Limit(1).IDs(setContextOp(ctx, sq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{s3user.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sq *S3UserQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := sq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single S3User entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one S3User entity is found.
// Returns a *NotFoundError when no S3User entities are found.
func (sq *S3UserQuery) Only(ctx context.Context) (*S3User, error) {
	nodes, err := sq.Limit(2).All(setContextOp(ctx, sq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{s3user.Label}
	default:
		return nil, &NotSingularError{s3user.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sq *S3UserQuery) OnlyX(ctx context.Context) *S3User {
	node, err := sq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only S3User ID in the query.
// Returns a *NotSingularError when more than one S3User ID is found.
// Returns a *NotFoundError when no entities are found.
func (sq *S3UserQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = sq.Limit(2).IDs(setContextOp(ctx, sq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{s3user.Label}
	default:
		err = &NotSingularError{s3user.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sq *S3UserQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := sq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of S3Users.
func (sq *S3UserQuery) All(ctx context.Context) ([]*S3User, error) {
	ctx = setContextOp(ctx, sq.ctx, "All")
	if err := sq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*S3User, *S3UserQuery]()
	return withInterceptors[[]*S3User](ctx, sq, qr, sq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sq *S3UserQuery) AllX(ctx context.Context) []*S3User {
	nodes, err := sq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of S3User IDs.
func (sq *S3UserQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if sq.ctx.Unique == nil && sq.path != nil {
		sq.Unique(true)
	}
	ctx = setContextOp(ctx, sq.ctx, "IDs")
	if err = sq.Select(s3user.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sq *S3UserQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := sq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sq *S3UserQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sq.ctx, "Count")
	if err := sq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sq, querierCount[*S3UserQuery](), sq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sq *S3UserQuery) CountX(ctx context.Context) int {
	count, err := sq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sq *S3UserQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sq.ctx, "Exist")
	switch _, err := sq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sq *S3UserQuery) ExistX(ctx context.Context) bool {
	exist, err := sq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the S3UserQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sq *S3UserQuery) Clone() *S3UserQuery {
	if sq == nil {
		return nil
	}
	return &S3UserQuery{
		config:      sq.config,
		ctx:         sq.ctx.Clone(),
		order:       append([]s3user.OrderOption{}, sq.order...),
		inters:      append([]Interceptor{}, sq.inters...),
		predicates:  append([]predicate.S3User{}, sq.predicates...),
		withBuckets: sq.withBuckets.Clone(),
		// clone intermediate query.
		sql:  sq.sql.Clone(),
		path: sq.path,
	}
}

// WithBuckets tells the query-builder to eager-load the nodes that are connected to
// the "buckets" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *S3UserQuery) WithBuckets(opts ...func(*S3BucketQuery)) *S3UserQuery {
	query := (&S3BucketClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withBuckets = query
	return sq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		FkUserID uuid.UUID `json:"fk_user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.S3User.Query().
//		GroupBy(s3user.FieldFkUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sq *S3UserQuery) GroupBy(field string, fields ...string) *S3UserGroupBy {
	sq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &S3UserGroupBy{build: sq}
	grbuild.flds = &sq.ctx.Fields
	grbuild.label = s3user.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		FkUserID uuid.UUID `json:"fk_user_id,omitempty"`
//	}
//
//	client.S3User.Query().
//		Select(s3user.FieldFkUserID).
//		Scan(ctx, &v)
func (sq *S3UserQuery) Select(fields ...string) *S3UserSelect {
	sq.ctx.Fields = append(sq.ctx.Fields, fields...)
	sbuild := &S3UserSelect{S3UserQuery: sq}
	sbuild.label = s3user.Label
	sbuild.flds, sbuild.scan = &sq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a S3UserSelect configured with the given aggregations.
func (sq *S3UserQuery) Aggregate(fns ...AggregateFunc) *S3UserSelect {
	return sq.Select().Aggregate(fns...)
}

func (sq *S3UserQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sq); err != nil {
				return err
			}
		}
	}
	for _, f := range sq.ctx.Fields {
		if !s3user.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sq.path != nil {
		prev, err := sq.path(ctx)
		if err != nil {
			return err
		}
		sq.sql = prev
	}
	return nil
}

func (sq *S3UserQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*S3User, error) {
	var (
		nodes       = []*S3User{}
		_spec       = sq.querySpec()
		loadedTypes = [1]bool{
			sq.withBuckets != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*S3User).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &S3User{config: sq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := sq.withBuckets; query != nil {
		if err := sq.loadBuckets(ctx, query, nodes,
			func(n *S3User) { n.Edges.Buckets = []*S3Bucket{} },
			func(n *S3User, e *S3Bucket) { n.Edges.Buckets = append(n.Edges.Buckets, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (sq *S3UserQuery) loadBuckets(ctx context.Context, query *S3BucketQuery, nodes []*S3User, init func(*S3User), assign func(*S3User, *S3Bucket)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*S3User)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.S3Bucket(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(s3user.BucketsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.s3bucket_user
		if fk == nil {
			return fmt.Errorf(`foreign-key "s3bucket_user" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "s3bucket_user" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (sq *S3UserQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sq.querySpec()
	_spec.Node.Columns = sq.ctx.Fields
	if len(sq.ctx.Fields) > 0 {
		_spec.Unique = sq.ctx.Unique != nil && *sq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sq.driver, _spec)
}

func (sq *S3UserQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(s3user.Table, s3user.Columns, sqlgraph.NewFieldSpec(s3user.FieldID, field.TypeUUID))
	_spec.From = sq.sql
	if unique := sq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sq.path != nil {
		_spec.Unique = true
	}
	if fields := sq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, s3user.FieldID)
		for i := range fields {
			if fields[i] != s3user.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sq *S3UserQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sq.driver.Dialect())
	t1 := builder.Table(s3user.Table)
	columns := sq.ctx.Fields
	if len(columns) == 0 {
		columns = s3user.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sq.sql != nil {
		selector = sq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sq.ctx.Unique != nil && *sq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sq.predicates {
		p(selector)
	}
	for _, p := range sq.order {
		p(selector)
	}
	if offset := sq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// S3UserGroupBy is the group-by builder for S3User entities.
type S3UserGroupBy struct {
	selector
	build *S3UserQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sgb *S3UserGroupBy) Aggregate(fns ...AggregateFunc) *S3UserGroupBy {
	sgb.fns = append(sgb.fns, fns...)
	return sgb
}

// Scan applies the selector query and scans the result into the given value.
func (sgb *S3UserGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sgb.build.ctx, "GroupBy")
	if err := sgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*S3UserQuery, *S3UserGroupBy](ctx, sgb.build, sgb, sgb.build.inters, v)
}

func (sgb *S3UserGroupBy) sqlScan(ctx context.Context, root *S3UserQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sgb.fns))
	for _, fn := range sgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sgb.flds)+len(sgb.fns))
		for _, f := range *sgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// S3UserSelect is the builder for selecting fields of S3User entities.
type S3UserSelect struct {
	*S3UserQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ss *S3UserSelect) Aggregate(fns ...AggregateFunc) *S3UserSelect {
	ss.fns = append(ss.fns, fns...)
	return ss
}

// Scan applies the selector query and scans the result into the given value.
func (ss *S3UserSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ss.ctx, "Select")
	if err := ss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*S3UserQuery, *S3UserSelect](ctx, ss.S3UserQuery, ss, ss.inters, v)
}

func (ss *S3UserSelect) sqlScan(ctx context.Context, root *S3UserQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ss.fns))
	for _, fn := range ss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
